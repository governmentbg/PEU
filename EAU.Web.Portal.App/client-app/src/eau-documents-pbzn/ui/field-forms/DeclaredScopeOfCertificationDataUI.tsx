import * as React from "react";
import { observer } from "mobx-react";
import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import { ApplicationForIssuingOrRenewingCertificateToWorkWithFluorinatedGreenhouseGassesEntityDataVM } from "../../models/ModelsAutoGenerated";
import { FieldFormUI } from "eau-documents";

interface DeclaredScopeOfCertificationDataUIProps extends BaseProps {
}

@observer
export class DeclaredScopeOfCertificationDataUI
    extends EAUBaseComponent<DeclaredScopeOfCertificationDataUIProps, ApplicationForIssuingOrRenewingCertificateToWorkWithFluorinatedGreenhouseGassesEntityDataVM> {
   
    constructor(props: DeclaredScopeOfCertificationDataUIProps) {
        super(props);

        //Bind     
       
        //Init
        
    }
    renderEdit() {
        return (
            <>
                <FieldFormUI title={this.getResourceByProperty(m => m.declaredScopeOfCertification)} required>
                    <div className="row">
                        <div className="form-group col-12">
                            {this.textAreaFor(m => m.declaredScopeOfCertification)}
                            {this.inlineHelpFor(m => m.declaredScopeOfCertification, 'GL_PBZN_DeclaredScopeOfCertificationHelp_L')}
                        </div>
                    </div>
                </FieldFormUI>
            </>
        );
    };

    renderDisplay() {
        return (
            <>
                <fieldset className="fields-group">
                    <legend>
                        <h3 className="field-title">
                            {this.getResourceByProperty(m => m.declaredScopeOfCertification)}
                        </h3>
                    </legend>
                    <div className="row">
                        <div className="form-group col-12">
                            {this.textDisplayFor(m => m.declaredScopeOfCertification)}
                        </div>
                    </div>
                </fieldset>
            </>
        );
    }  
}
