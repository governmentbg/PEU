import * as React from "react";
import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import { PersonEntityDataVM } from "../../models/ModelsAutoGenerated";
import { PersonDataUI, EntityDataUI } from ".";

export class ReportForChangingOwnershipOwnerItemUI extends EAUBaseComponent<BaseProps, PersonEntityDataVM>{
    constructor(props: BaseProps) {
        super(props);

    }


    render(): JSX.Element {
        return (
            <>
                {this.model.person ?
                    <PersonDataUI {...this.bind(m => m.person)} />
                    : null}
                {this.model.entity ?
                    <EntityDataUI {...this.bind(m => m.entity)} />
                    : null}
            </>
        )
    }
}