import * as React from "react";
import { observer } from "mobx-react";
import { BaseProps, ViewMode } from "cnsys-ui-react";
import { EAUBaseComponent, Constants } from "eau-core";
import { Status } from "../../models/ModelsAutoGenerated";

@observer export class StatusUI extends EAUBaseComponent<BaseProps, Status> {

    render(): JSX.Element {
        return (
            <>
                {this.textDisplayFor(m => m.description)}
            </>
        );
    }
}