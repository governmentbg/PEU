import { BaseProps } from "cnsys-ui-react";
import { Constants, EAUBaseComponent } from "eau-core";
import { isMoment } from "moment";
import React from "react";
import { ReportForChangingOwnershipV2VehicleDataPeriodicTechnicalCheck, ReportForVehiclePeriodicTechnicalCheck } from "../../models/ModelsAutoGenerated";
import { DisplayStatusesUI } from "./DisplayStatusesUI";

export class PeriodicTechnicalCheckUI extends EAUBaseComponent<BaseProps, ReportForVehiclePeriodicTechnicalCheck | ReportForChangingOwnershipV2VehicleDataPeriodicTechnicalCheck> {

    render(): JSX.Element {
        return (
            <>
                {
                    isMoment(this.model.nextInspectionDate)
                        ? <div className="row">
                            <div className="col-sm-6 form-group">
                                <h4 className="form-control-label">{this.getResourceByProperty(m => m.nextInspectionDate)}</h4>
                                {this.dateDisplayFor(this.model.nextInspectionDate, Constants.DATE_FORMATS.date)}
                            </div>
                        </div>
                        : null
                }
                <DisplayStatusesUI statuses={this.model.status} />
            </>
        );
    }
}