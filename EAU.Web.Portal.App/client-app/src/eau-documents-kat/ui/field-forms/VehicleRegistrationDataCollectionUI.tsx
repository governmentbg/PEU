import * as React from "react";
import { observer } from "mobx-react";
import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import { VehicleRegistrationDataVM, RegistrationCertificateTypeNomenclature } from "../../models/ModelsAutoGenerated";
import { FieldFormUI, withCollectionItems, PersonIdentifierChoiceType } from "eau-documents";
import { OwnerUI, VehicleRegistrationDataUI } from ".";


interface VehicleRegistrationDataProps extends BaseProps {

}

@observer export class VehicleRegistrationDataImpl extends EAUBaseComponent<VehicleRegistrationDataProps, VehicleRegistrationDataVM> {

    renderEdit(): JSX.Element {
        return (
            <>
                <VehicleRegistrationDataUI {...this.bind(m => m)} />
            </>
        );
    }

    renderDisplay(): JSX.Element {
        return (
            <>
                <VehicleRegistrationDataUI {...this.bind(m => m)} />
            </>
        );
    }
}

export const VehicleRegistrationDataCollectionUI = withCollectionItems(VehicleRegistrationDataImpl, {
    initItem: () => {
        let newVehicle = new VehicleRegistrationDataVM();
        newVehicle.registrationCertificateType = RegistrationCertificateTypeNomenclature.RegistrationDocument;
        return newVehicle;
    },
    addButtonLabelKey: 'GL_ADD_VECHICLE_L'
});

