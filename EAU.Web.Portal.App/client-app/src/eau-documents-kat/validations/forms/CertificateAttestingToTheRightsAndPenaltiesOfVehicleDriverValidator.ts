import { EAUBaseValidator, ErrMsgCodesConstants, ResourceHelpers, resourceManager } from "eau-core";
import { DocumentFormValidationContext } from "eau-documents";
import { CertificateAttestingToTheRightsAndPenaltiesOfVehicleDriverVM } from "../../models/ModelsAutoGenerated";

export class CertificateAttestingToTheRightsAndPenaltiesOfVehicleDriverValidator extends EAUBaseValidator<CertificateAttestingToTheRightsAndPenaltiesOfVehicleDriverVM, DocumentFormValidationContext> {
    constructor() {
        super();

        this.ruleFor(m => m.certificateData).notEmpty()
            .withMessage(ResourceHelpers.formatErrorMessage(ErrMsgCodesConstants.DefaultNotEmptyErrorMessage, new CertificateAttestingToTheRightsAndPenaltiesOfVehicleDriverVM(), 'certificateData'));

        this.ruleFor(m => m.reportDate).isValidDate().withMessage(resourceManager.getResourceByKey("DOC_GL_WRONG_DATE_AND_TIME_E"));
        this.ruleFor(m => m.reportDate).notEmpty()
            .withMessage(ResourceHelpers.formatErrorMessage(ErrMsgCodesConstants.DefaultNotEmptyErrorMessage, new CertificateAttestingToTheRightsAndPenaltiesOfVehicleDriverVM(), 'reportDate'));
    }
}