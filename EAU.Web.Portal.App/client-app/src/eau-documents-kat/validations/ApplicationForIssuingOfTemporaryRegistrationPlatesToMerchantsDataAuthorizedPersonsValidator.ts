import { EAUBaseValidator, ErrMsgCodesConstants, ResourceHelpers } from "eau-core";
import { DocumentFormValidationContext, PersonIdentifierValidator } from "eau-documents";
import { ApplicationForIssuingOfTemporaryRegistrationPlatesToMerchantsDataAuthorizedPersons } from "../models/ModelsAutoGenerated";

export class ApplicationForIssuingOfTemporaryRegistrationPlatesToMerchantsDataAuthorizedPersonsValidator extends EAUBaseValidator<ApplicationForIssuingOfTemporaryRegistrationPlatesToMerchantsDataAuthorizedPersons, DocumentFormValidationContext> {
    constructor() {
        super();

        this.ruleFor(m => m.fullName).notEmpty()
            .withMessage(ResourceHelpers.formatErrorMessage(ErrMsgCodesConstants.DefaultNotEmptyErrorMessage, new ApplicationForIssuingOfTemporaryRegistrationPlatesToMerchantsDataAuthorizedPersons(), 'fullName'));

        this.ruleFor(m => m.fullName).length(0, 150)
            .withMessage(ResourceHelpers.formatErrorMessage(ErrMsgCodesConstants.FieldCanNotContainsMoreThanSymbols, ApplicationForIssuingOfTemporaryRegistrationPlatesToMerchantsDataAuthorizedPersons, 'fullName', 150));

        this.ruleFor(m => m.identifier).setValidator(new PersonIdentifierValidator());
    }
}