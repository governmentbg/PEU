import { EAUBaseValidator } from "eau-core";
import { DocumentFormValidationContext } from "eau-documents";
import { ApplicationForWithdrawServiceDataVM } from "../models/ModelsAutoGenerated";

export class ApplicationForWithdrawServiceDataValidator
    extends EAUBaseValidator<ApplicationForWithdrawServiceDataVM, DocumentFormValidationContext> {

    constructor() {
        super();

        this.ruleFor(m => m.caseFileURI).notEmpty().withMessage(this.getMessage('GL_INPUT_FIELD_MUST_E'));
        this.ruleFor(m => m.issuingDocument).notEmpty().withMessage(this.getMessage('GL_INPUT_FIELD_MUST_E'));
        this.ruleFor(m => m.policeDepartment).notEmpty().withMessage(this.getMessage('GL_INPUT_FIELD_MUST_E'));
    }
}