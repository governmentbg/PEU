import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent, ResourceHelpers } from "eau-core";
import { observer } from "mobx-react";
import React from "react";
import { ElectronicServiceApplicantVM } from "../../models/ModelsAutoGenerated";
import { RecipientGroupUI } from "../field-forms/RecipientGroupUI";

interface ElectronicServiceApplicantUIProps extends BaseProps {
}

@observer export class ElectronicServiceApplicantUI extends EAUBaseComponent<ElectronicServiceApplicantUIProps, ElectronicServiceApplicantVM> {
    constructor(props: ElectronicServiceApplicantUIProps) {
        super(props);
    }

    renderEdit(): JSX.Element {
        return (
            <>
                <RecipientGroupUI {...this.bind(m => m.recipientGroup)} />
            </>
        );
    }

    renderDisplay(): JSX.Element {
        return (
            <>
                <RecipientGroupUI {...this.bind(m => m.recipientGroup)} />
                {this.model.sendApplicationWithReceiptAcknowledgedMessage === true
                    && <fieldset className="fields-group">
                        <div className="row mt-2">
                            <div className="form-group col-12">
                            <p className="field-text check-item check-success">
                                {this.getResourceByProperty(m => m.sendApplicationWithReceiptAcknowledgedMessage)}
                                </p>
                            </div>
                        </div>
                    </fieldset>
                }
            </>
        );
    }
}