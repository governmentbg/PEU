import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import { observer } from "mobx-react";
import React from "react";
import { ElectronicServiceApplicantVM } from "../../models/ModelsAutoGenerated";
import { RecipientGroupAuthorUI } from "..";

interface DeptorUIUIProps extends BaseProps {
}

@observer export class DeptorUI extends EAUBaseComponent<DeptorUIUIProps, ElectronicServiceApplicantVM> {
    constructor(props: DeptorUIUIProps) {
        super(props);
    }
    
    render(): JSX.Element {
        return (
            <>
                <RecipientGroupAuthorUI {...this.bind(m => m.recipientGroup)} />                
            </>
        );
    }
}