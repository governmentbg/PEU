import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import { observer } from "mobx-react";
import React from "react";
import { RecipientGroupVM } from "../../models/ModelsAutoGenerated";
import { AuthorUI } from ".";

@observer export class RecipientGroupAuthorUI extends EAUBaseComponent<BaseProps, RecipientGroupVM> {
    constructor(props: BaseProps) {
        super(props);
    }

    render(): JSX.Element {
        return (
            <>
                <AuthorUI {...this.bind(m => m.recipient)} /> 
            </>
        );
    }
}