import { BaseProps } from "cnsys-ui-react";
import { attributesClassFormControl, attributesClassFormControlLabel, attributesClassFormControlRequiredLabel, EAUBaseComponent } from "eau-core";
import React from "react";
import { IssuerCountryVM } from "../../models/ModelsAutoGenerated";

export class IssuerCountryUI extends EAUBaseComponent<BaseProps, IssuerCountryVM> {

    renderEdit() {
        return <div className="form-group col-sm-6 col-lg-3">
            {this.labelFor(m => m.countryName, null, attributesClassFormControlRequiredLabel)}
            {this.textBoxFor(m => m.countryName, attributesClassFormControl)}
        </div>
    }

    renderDisplay() {
        return <div className="form-group col-sm-6 col-lg-3">
            {this.labelFor(m => m.countryName, null, attributesClassFormControlLabel)}
            {this.textDisplayFor(m => m.countryName)}
        </div>
    }
}