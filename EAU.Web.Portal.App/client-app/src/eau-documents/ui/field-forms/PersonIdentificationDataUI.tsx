import { BaseProps, ViewMode } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import React from "react";
import { PersonIdentificationData } from "../../models/ModelsAutoGenerated";
import { GenderDataUI } from "./GenderDataUI";
import { PersonIdentifierUI } from "./PersonIdentifierUI";

interface PersonIdentificationDataProps extends BaseProps {
    placeOfBirth: string;
}

export class PersonIdentificationDataUI extends EAUBaseComponent<PersonIdentificationDataProps, PersonIdentificationData> {

    renderEdit(): JSX.Element {
        return <>
            <div className="row">
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.names)}</h4>
                    <p className="field-text">{this.model.names
                        && `${this.model.names.first || ""} ${this.model.names.middle || ""} ${this.model.names.last || ""}`}</p>
                </div>
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.namesLatin)}</h4>
                    <p className="field-text">{this.model.namesLatin
                        && `${this.model.namesLatin.first || ""} ${this.model.namesLatin.middle || ""} ${this.model.namesLatin.last || ""}`}</p>
                </div>
            </div>
            <div className="row">
                <PersonIdentifierUI {...this.bind(m => m.identifier)} viewMode={ViewMode.Display} />
                <GenderDataUI {...this.bind(m => m.gender)} />
            </div>
            <div className="row">
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.birthDate)}</h4>
                    <p className="field-text">{this.dateDisplayFor(this.model.birthDate)}</p>
                </div>
                <div className="form-group col-sm-8">
                    <h4 className="form-control-label">{this.getResource("DOC_GL_BIRTH_PLACE_L")}</h4>
                    <p className="field-text">{this.props.placeOfBirth}</p>
                </div>
            </div>
        </>
    }

    renderDisplay(): JSX.Element {
        return <>
            <div className="row">
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.names)}</h4>
                    <p className="field-text">{this.model.names
                        && `${this.model.names.first || ""} ${this.model.names.middle || ""} ${this.model.names.last || ""}`}</p>
                </div>
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.namesLatin)}</h4>
                    <p className="field-text">{this.model.namesLatin
                        && `${this.model.namesLatin.first || ""} ${this.model.namesLatin.middle || ""} ${this.model.namesLatin.last || ""}`}</p>
                </div>
            </div>
            <div className="row">
                <PersonIdentifierUI {...this.bind(m => m.identifier)} viewMode={ViewMode.Display} />
                <GenderDataUI {...this.bind(m => m.gender)} />
            </div>
            <div className="row">
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.birthDate)}</h4>
                    <p className="field-text">{this.dateDisplayFor(this.model.birthDate)}</p>
                </div>
                <div className="form-group col-sm-8">
                    <h4 className="form-control-label">{this.getResource("DOC_GL_BIRTH_PLACE_L")}</h4>
                    <p className="field-text">{this.props.placeOfBirth}</p>
                </div>
            </div>
        </>
    }
}