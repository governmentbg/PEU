import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent } from "eau-core";
import { observer } from "mobx-react";
import * as React from "react";
import { RemovingIrregularitiesInstructionsIrregularitiesVM } from "../../models/ModelsAutoGenerated";
import { withCollectionItems } from "../CollectionItemsContainer";
import { RemovingIrregularitiesInstructionsIrregularityUI } from "./RemovingIrregularitiesInstructionsIrregularityUI";

@observer export class IrregularitiesInstructionsImpl extends EAUBaseComponent<BaseProps, RemovingIrregularitiesInstructionsIrregularitiesVM> {

    renderEdit(): JSX.Element {
        return (
            <>
                <RemovingIrregularitiesInstructionsIrregularityUI {...this.bind(m => m)} />
            </>
        );
    }

    renderDisplay(): JSX.Element {
        return (
            <>
                <RemovingIrregularitiesInstructionsIrregularityUI {...this.bind(m => m)} />
            </>
        );
    }
}

export const IrregularitiesInstructionsCollectionsUI = withCollectionItems( IrregularitiesInstructionsImpl, {
    initItem: () => {
        let newIrregularity = new RemovingIrregularitiesInstructionsIrregularitiesVM();
        newIrregularity.additionalInformationSpecifyingIrregularity = "";
        newIrregularity.irregularityType = "";

        return newIrregularity;
    },
    addButtonLabelKey: 'DOC_GL_RemovingIrregularitiesInstructions_addIrregularity_L'
});

