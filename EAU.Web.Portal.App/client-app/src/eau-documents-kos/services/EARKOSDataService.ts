import { EAUBaseDataService } from "eau-core";
import { LicenseInfo } from "../models/ModelsAutoGenerated";

export class EARKOSDataService extends EAUBaseDataService {

    protected baseUrl(): string {
        return super.baseUrl() + "EARKOS";
    }

    public getLicenseData(permitNumber: string): Promise<LicenseInfo> {
        return this.get<LicenseInfo>(`/${permitNumber}`, LicenseInfo);
    }
}