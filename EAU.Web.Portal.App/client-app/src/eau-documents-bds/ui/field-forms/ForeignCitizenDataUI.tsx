import { ObjectHelper } from "cnsys-core";
import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent, attributesClassFormControlLabel } from "eau-core";
import React from "react";
import { ForeignCitizenDataVM } from "../../models/ModelsAutoGenerated";

interface ForeignCitizenDataProps extends BaseProps {
    egn: string;
    lnch: string;
}

export class ForeignCitizenDataUI extends EAUBaseComponent<ForeignCitizenDataProps, ForeignCitizenDataVM> {

    render() {
        return <>
            <div className="row">
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResource("DOC_BDS_NAMES_PERSONAL_CARD_PASSPORT_L")}</h4>
                    <p className="field-text">{this.model.foreignCitizenNames
                        && `${this.model.foreignCitizenNames.firstLatin || ""} ${this.model.foreignCitizenNames.otherLatin || ""} ${this.model.foreignCitizenNames.lastLatin || ""}`}</p>
                </div>
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResource("DOC_BDS_NAMES_CYRILLIC_L")}</h4>
                    <p className="field-text">{this.model.foreignCitizenNames
                        && `${this.model.foreignCitizenNames.firstCyrillic || ""} ${this.model.foreignCitizenNames.otherCyrillic || ""} ${this.model.foreignCitizenNames.lastCyrillic || ""}`}</p>
                </div>
            </div>
            <div className="row">
                {
                    !ObjectHelper.isStringNullOrEmpty(this.props.lnch) && <div className="form-group col-sm-4">
                        <h4 className="form-control-label">{this.getResource("GL_LNCH_L")}</h4>
                        <p className="field-text">{this.props.lnch}</p>
                    </div>
                }
                {
                    !ObjectHelper.isStringNullOrEmpty(this.props.egn) && <div className="form-group col-sm-4">
                        <h4 className="form-control-label">{this.getResource("GL_EGN_L")}</h4>
                        <p className="field-text">{this.props.egn}</p>
                    </div>
                }
                <div className="form-group col-sm-4">
                    {this.labelFor(m => m.genderName, null, attributesClassFormControlLabel)}
                    {this.textDisplayFor(m => m.genderName)}
                </div>
            </div>
            <div className="row">
                <div className="form-group col-sm-4">
                    {this.labelFor(m => m.birthDate, null, attributesClassFormControlLabel)}
                    <p className="field-text">{this.model.birthDate}</p>
                </div>
                <div className="form-group col-sm-4">
                    <h4 className="form-control-label">{this.getResourceByProperty(m => m.citizenship)}</h4>
                    <p className="field-text">{this.model.citizenship && this.model.citizenship.countryName}</p>
                </div>
            </div>
        </>
    }
}