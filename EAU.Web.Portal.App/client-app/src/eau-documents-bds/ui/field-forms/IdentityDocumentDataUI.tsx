import { BaseProps } from "cnsys-ui-react";
import { EAUBaseComponent, Constants, ResourceHelpers } from "eau-core";
import React from "react";
import { IdentityDocumentData } from "../../models/ModelsAutoGenerated";
import { IdentityDocumentType } from "eau-documents";

export class IdentityDocumentDataUI extends EAUBaseComponent<BaseProps, IdentityDocumentData> {

    render(): JSX.Element {
        return (
            <>
                <div className="row">
                    <div className="form-group col-12">
                        <h4 className="form-control-label">{this.getResourceByProperty(m => m.identityDocumentType)}</h4>
                        {ResourceHelpers.getResourceByEmun(this.model.identityDocumentType, IdentityDocumentType)}
                    </div>
                </div>
                <div className="row">
                    <div className="form-group col-sm-6 col-lg-3">
                        <h4 className="form-control-label">{this.getResourceByProperty(m => m.identityNumber)}</h4>
                        {this.textDisplayFor(m => m.identityNumber)}
                    </div>
                    <div className="form-group col-sm-6 col-lg-3">
                        <h4 className="form-control-label">{this.getResourceByProperty(m => m.identitityIssueDate)}</h4>
                        {this.dateDisplayFor(this.model.identitityIssueDate, Constants.DATE_FORMATS.date)}
                    </div>
                    <div className="form-group col-sm-6 col-lg-3">
                        <h4 className="form-control-label">{this.getResourceByProperty(m => m.identitityExpireDate)}</h4>
                        {this.dateDisplayFor(this.model.identitityExpireDate, Constants.DATE_FORMATS.date)}
                    </div>
                    <div className="form-group col-sm-6 col-lg-3">
                        <h4 className="form-control-label">{this.getResourceByProperty(m => m.identityIssuer)}</h4>
                        {this.textDisplayFor(m => m.identityIssuer)}
                    </div>
                </div>
                <div className="row">
                    <div className="form-group col-sm-6 col-lg-3">
                        <h4 className="form-control-label">{this.getResourceByProperty(m => m.identityDocumentStatus)}</h4>
                        {this.textDisplayFor(m => m.identityDocumentStatus)}
                    </div>
                </div>                                  
            </>
        )
    }
}