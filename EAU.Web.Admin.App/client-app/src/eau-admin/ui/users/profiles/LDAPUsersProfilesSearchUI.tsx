import { BaseProps } from 'cnsys-ui-react';
import { EAUBaseComponent } from 'eau-core';
import React from 'react';
import { LDAPUserSearchCritaria } from '../../../models/ModelsAutoGenerated';

interface LDAPUsersProfilesSearchProps extends BaseProps {
}

class LDAPUsersProfilesSearchUI extends EAUBaseComponent<LDAPUsersProfilesSearchProps, LDAPUserSearchCritaria>{

    render() {

        return <>
            <div className="card-header">
                <h3>{this.getResource("GL_SEARCH_USER_L")}</h3>
            </div>
            <div className="card-body">
                <div className="row">
                    <div className="form-group col-sm-4">
                        {this.labelFor(x => x.username, "GL_USERNAME_L")}
                        {this.textBoxFor(x => x.username)}
                    </div>
                    <div className="form-group col-sm-4">
                        {this.labelFor(x => x.firstName, "GL_NAME_L")}
                        {this.textBoxFor(x => x.firstName)}
                    </div>
                    <div className="form-group col-sm-4">
                        {this.labelFor(x => x.lastName, "GL_LASTNAME_L")}
                        {this.textBoxFor(x => x.lastName)}
                    </div>
                </div>
            </div>
        </>
    }
}

export default LDAPUsersProfilesSearchUI;