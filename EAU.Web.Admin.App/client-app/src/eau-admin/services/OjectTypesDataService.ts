import { ObjectType } from 'eau-admin/models/ModelsAutoGenerated';
import { EAUBaseDataService } from '../../eau-core/services/EAUBaseDataService';

export class OjectTypesDataService extends EAUBaseDataService {

    protected baseUrl(): string {
        return super.baseUrl() + "ObjectTypes";
    }

    public getOjectTypes(): Promise<ObjectType[]> {
        return this.get<ObjectType[]>(``, ObjectType, null);
    }
}