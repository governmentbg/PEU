import { ActionType } from '../models/ModelsAutoGenerated';
import { EAUBaseDataService } from '../../eau-core/services/EAUBaseDataService';

export class ActionTypesDataService extends EAUBaseDataService {

    protected baseUrl(): string {
        return super.baseUrl() + "ActionTypes";
    }

    public getOjectTypes(): Promise<ActionType[]> {
        return this.get<ActionType[]>(``, ActionType, null);
    }
}